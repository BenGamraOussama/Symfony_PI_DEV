{% extends 'basePatient.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center mb-4">🛒 Mon Panier</h1>

        {% if panierItems is not empty %}
            <div class="table-responsive">
                <table class="table table-striped text-center">
                    <thead class="table-dark">
                    <tr>
                        <th>Nom du produit</th>
                        <th>Quantité</th>
                        <th>Prix unitaire</th>
                        <th>Total</th>
                        <th>Action</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for item in panierItems %}
                        <tr>
                            <td>{{ item.produit.nom }}</td>
                            <td>{{ item.quantite }}</td>
                            <td>{{ item.produit.prix }} €</td>
                            <td>{{ item.produit.prix * item.quantite }} €</td>
                            <td>
                                <a href="{{ path('supprimer_du_panier', {'id': item.id}) }}" class="btn btn-danger btn-sm">
                                    <i class="fas fa-trash"></i> Supprimer
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>

            <div class="d-flex justify-content-between align-items-center mt-4">
                <h3 class="text-success">Total : <strong>{{ total }} €</strong></h3>
                <a href="{{ path('app_commande_index') }}" class="btn btn-primary btn-lg">
                    <i class="fas fa-shopping-cart"></i> Commander
                </a>
            </div>
        {% else %}
            <div class="alert alert-warning text-center mt-4">
                <p>Votre panier est vide.</p>
            </div>
        {% endif %}
    </div>
{% endblock %}
