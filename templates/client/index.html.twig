{% extends 'base.html.twig' %}

{% block title %}Hello ClientController!{% endblock %}

<h1>Nos Produits</h1>
<div class="product-list">
    {% for product in products %}
        <div class="product-item">
            <h2>{{ product.name }}</h2>
            <p>{{ product.description }}</p>
            <p>Prix: {{ product.price }}€</p>
            <form action="{{ path('order_product', {'id': product.id}) }}" method="post">
                <button type="submit">Commander</button>
            </form>
        </div>
    {% endfor %}
</div>

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>C:/Users/OUSSAMA BEN GAMRA/OneDrive/Desktop/symfony_pi_dev/src/Controller/ClientController.php</code></li>
        <li>Your template at <code>C:/Users/OUSSAMA BEN GAMRA/OneDrive/Desktop/symfony_pi_dev/templates/client/index.html.twig</code></li>
    </ul>
</div>
{% endblock %}
