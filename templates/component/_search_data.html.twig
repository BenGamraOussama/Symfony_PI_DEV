
            {{ form_start(form) }}
                <div class="flex justify-center">
                    <div class="mb-3 xl:w-96">
                    <div class="input-group relative flex flex-wrap items-stretch w-full mb-4">
                        {{ form_widget(form.q) }}
                    </div>
                </div>
              
                
                <button type="submit" class="btn inline-block px-6 py-2.5 bg-blue-600 text-white font-medium form-control">
                    <i class="bx bx-save"></i> {{ button_label|default('Update') }}
                </button>
            {{ form_end(form) }}
