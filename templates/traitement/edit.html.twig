{% extends 'base.html.twig' %}

{% block title %}Edit Traitement{% endblock %}

{% block body %}
    <h1>Edit Traitement</h1>

    {{ include('traitement/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_traitement_index', {'consultation_id': consultation.id}) }}" class="btn btn-secondary">Retour à la liste</a>


    {{ include('traitement/_delete_form.html.twig') }}
{% endblock %}
