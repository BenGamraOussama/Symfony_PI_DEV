{% extends 'base.html.twig' %}

{% block title %}New Traitement{% endblock %}

{% block body %}
    <h1>Create new Traitement</h1>

    {{ include('traitement/_form.html.twig') }}

       <a href="{{ path('app_traitement_index', {'consultation_id': consultation.id}) }}" class="btn btn-secondary">Retour à la liste</a>

{% endblock %}
